name: Threat Intel Feed Automation

on:
  schedule:
    - cron: "0 */6 * * *"   # every 6 hours
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        pip install requests feedgen

    - name: Run threat intel feed script
      env:
        OTX_API_KEY: ${{ secrets.OTX_API_KEY }}
      run: |
        python Threat_Intel_feed.py

    - name: Commit and push updated feed
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "actions@github.com"
        git add feeds/threat_intel.xml
        git commit -m "Auto-update threat intel feed" || echo "No changes"
        git push
